{
  "name": "tui-tools-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The tui-tools family website.",
  "scripts": {
    "catalog": "node scripts/build-catalog.mjs",
    "og": "node scripts/build-og.mjs",
    "dev": "npm run catalog && npm run og && astro dev",
    "build": "npm run catalog && npm run og && astro build",
    "build:only": "astro build",
    "preview": "astro preview"
  },
  "devDependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource/inter": "^5.3.0",
    "@fontsource/jetbrains-mono": "^5.3.0",
    "@resvg/resvg-js": "^2.6.2",
    "astro": "^7.2.8",
    "satori": "^0.33.4"
  }
}
